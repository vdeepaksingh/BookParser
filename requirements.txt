# Phase 1 — Ingestion
pymupdf>=1.24.0

# Phase 2 — Embedding & Vector Store
sentence-transformers>=3.0.0
qdrant-client>=1.9.0

# Phase 3 — Knowledge Graph
networkx>=3.3
stanza>=1.9.0
# Stanza model auto-downloads on first run (en tokenize+ner)

# Phase 4 — RAG
llama-index-core>=0.10.0
llama-index-llms-ollama>=0.1.0
llama-index-embeddings-huggingface>=0.2.0
rank-bm25>=0.2.2
sentence-transformers>=3.0.0  # CrossEncoder re-ranker
qdrant-client>=1.9.0

# Phase 5 — API & UI
fastapi>=0.111.0
uvicorn>=0.30.0
streamlit>=1.35.0
pyvis>=0.3.2
